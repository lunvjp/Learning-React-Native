import { ADD_NEW_WORD } from './type';
import {urls} from "../../config";

export const submitAnswerToQuestion = async () => {
  const response = await fetch();

}
// submitAnswerToQuestion
// sendMessage
