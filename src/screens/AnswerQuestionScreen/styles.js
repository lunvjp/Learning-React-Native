import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  questionItemTitle : {
    // fontSize : 30,
    fontWeight : 'bold',
    color : '#202124'
  },
});

export default styles;
